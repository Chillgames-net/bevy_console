# chill_bevy_console_derive

Proc-macro support for `chill_bevy_console`'s opt-in
`#[derive(ConsoleResource)]` API. Use it through the main crate's
`resource-properties` feature rather than depending on this crate directly.
